# Copy to .dev.vars for `npm run preview` (local Cloudflare Workers runtime).
# No server-side secrets are needed right now: the booking form posts straight
# to HubSpot, and the scheduler is a Calendly embed.
